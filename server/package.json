{
  "name": "graphql-server",
  "version": "1.0.0",
  "description": "GraphQL implementation for ImagesDB project.",
  "main": "src/app.js",
  "scripts": {
    "compile": "tsc",
    "dev": "ts-node-dev --respawn ./src/app.ts",
    "start": "npm run compile && npm start ./dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Firas",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.24",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@apollo/datasource-rest": "^6.2.2",
    "@apollo/server": "^4.10.0",
    "graphql-tag": "^2.12.6"
  }
}
